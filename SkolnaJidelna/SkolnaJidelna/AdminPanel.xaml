<Window x:Class="SkolniJidelna.AdminPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SkolniJidelna"
        mc:Ignorable="d"
        Title="AdminPanel" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель: список заказов -->
        <Grid Grid.Column="0">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF6B9E" Offset="0"/>
                    <GradientStop Color="#FF8B5E" Offset="0.5"/>
                    <GradientStop Color="#FFB66E" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Canvas>
                <Ellipse Width="150" Height="150" Fill="#30FFFFFF" Canvas.Left="-50" Canvas.Top="100"/>
                <Ellipse Width="200" Height="200" Fill="#20FFFFFF" Canvas.Left="200" Canvas.Top="400"/>
            </Canvas>

            <StackPanel VerticalAlignment="Top" Margin="40" Panel.ZIndex="1">
                <TextBlock Text="Seznam stravniku" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,20"/>

                <!-- Фильтр по типу стравника -->
                <TextBlock Text="Filtrovat podle typu stravnika" FontSize="16" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <ComboBox x:Name="comboStatusFilter" Style="{StaticResource ComboBoxStyle}" Margin="0,0,0,20" SelectionChanged="FilterChanged">
                    <ComboBoxItem Content="Vše"/>
                    <ComboBoxItem Content="Studenti"/>
                    <ComboBoxItem Content="Pracovnici"/>
                </ComboBox>

                <!-- Список стравников -->
                <ListBox x:Name="listBoxStravnici" Style="{StaticResource ListBoxStyle}" Height="400" SelectionChanged="OrderSelected">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10" Background="White" Padding="10" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="{Binding Role, StringFormat='Role:{null'}" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding TypStravnik, StringFormat='Typ: {0} Kč'}" FontSize="12" Foreground="#757575"/>
                                    <TextBlock Text="{Binding Email, StringFormat='Email: {0}'}" FontSize="12" Foreground="#999999"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button Content="Zpět do profilu" Style="{StaticResource LightButton}" Margin="0,40,0,0" Click="BackButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Правая панель: детали выбранного стравника -->
        <Grid Grid.Column="1">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FFF5F8" Offset="0"/>
                    <GradientStop Color="#FFF9F0" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Canvas>
                <Ellipse Width="120" Height="120" Fill="#FFE5B430" Canvas.Right="50" Canvas.Top="80"/>
                <Ellipse Width="80" Height="80" Fill="#FFB6D930" Canvas.Right="200" Canvas.Top="500"/>
            </Canvas>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Panel.ZIndex="1">
                <StackPanel VerticalAlignment="Top" Margin="70,40" Panel.ZIndex="1">
                    <TextBlock Text="Detaily stravniku" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,30">
                        <TextBlock.Foreground>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#FF6B9E" Offset="0"/>
                                <GradientStop Color="#FF8B5E" Offset="1"/>
                            </LinearGradientBrush>
                        </TextBlock.Foreground>
                    </TextBlock>

                    <!-- Детали конкретного стравника -->
                    <TextBlock x:Name="textJmeno" Text="Jmeno: -" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="textPrijmeni" Text="Prijmeni: -" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="textRole" Text="Role: -" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="textTyp" Text="Typ stravniku: -" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="textEmail" Text="Email: -" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="textHeslo" Text="Heslo: -" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="textAktivita" Text="Aktivita: -" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="textZustatek" Text="Zustatek: -" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <!-- <TextBlock x:Name="textOrderTotal" Text="Celková cena: 0 Kč" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/> -->
                    <TextBlock x:Name="textOrderNote" Text="Poznámka: -" FontSize="14" Foreground="#999999" Margin="0,0,0,30"/>

                    <!-- Список блюд -->
                    <TextBlock Text="Alergie:" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <ItemsControl x:Name="itemsControlOrderDetails" Margin="0,0,0,20">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border CornerRadius="10" Background="#F0F0F0" Padding="10" Margin="0,0,0,10">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Nazev}" FontSize="16" FontWeight="Bold"/>
                                        <!-- <TextBlock Text="{Binding Cena, StringFormat='Cena: {0} Kč'}" FontSize="14" Foreground="#757575"/>  -->
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>

</Window>
